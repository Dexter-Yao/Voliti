# ABOUTME: 评估运行默认参数
# ABOUTME: 可通过 CLI 参数覆盖

server_url: "http://localhost:2025"
assistant_id: "coach"
max_turns_default: 20
turn_timeout_seconds: 300
seed_directory: "seeds/"
seed_directory_lite: "seeds_lite/"
seed_directory_smoke: "seeds_smoke/"
profile_manifest: "config/profiles.yaml"
output_directory: "output/"
max_concurrency: 10

model_labels:
  coach: "GPT-5.4"
  coach_qwen: "Qwen 3.6 Plus"
